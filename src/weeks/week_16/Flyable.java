package weeks.week_16;

public interface Flyable {

    // 1 - Interface can have only public abstract methods and constant fields (final)
    // 2 - A class can implement multiple interfaces but can only extend one class

    int length = 5;
     void fly();
}
